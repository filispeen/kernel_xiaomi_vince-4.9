name: HackKernel
on:
  workflow_dispatch:
  
jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: najahi/oven:latest
    steps:
      - name: Setup
        run: |
          #rm -rf  ~/../etc/apt/sources.list
          #echo "deb http://old-releases.ubuntu.com/ubuntu/ groovy main restricted universe multiverse\ndeb http://old-releases.ubuntu.com/ubuntu/ groovy-updates main restricted universe multiverse\ndeb http://old-releases.ubuntu.com/ubuntu/ groovy-security main restricted universe multiverse" >> ../../etc/apt/sources.list
          cat ~/../etc/apt/sources.list
          apt update -y && apt upgrade -y
          apt install libssl-dev -y
      - name: Checkout
        uses: actions/checkout@v2
      - name: Compile Kernel
        run: |
          wget https://raw.githubusercontent.com/Kry9toN/Scripts/master/kernel/CI/vince/semaphore49
          bash semaphore49
